# Server Configuration
server.port=8080
server.servlet.context-path=/api

# NeonDB PostgreSQL Configuration
spring.datasource.url=${NEON_DB_URL}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${NEON_DB_USERNAME}
spring.datasource.password=${NEON_DB_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Schema initialization
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
file.upload-dir=./uploads

# JWT Configuration
app.jwt.secret=a8d4f6g7h9j0k2m3n5p6q8r9s1t2v3w4x5z7a8b9c1d2e3f4g5h6j7k8l9m0
app.jwt.expiration=86400000
app.jwt.header=Authorization
app.jwt.prefix=Bearer 

# Logging
logging.level.root=INFO
logging.level.io.github.some_example_name.server=DEBUG
logging.level.org.springframework.security=DEBUG
